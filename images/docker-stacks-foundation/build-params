source ${HOME}/build/tools/build-params
source ../build-params

UBUNTU_VERSION="20.04"

BASE_REPOSITORY="jupyter/docker-stacks-foundation:latest"

BASE_CONTAINER="${BASE_REGISTRY}/ubuntubuild:${UBUNTU_VERSION}"
BASE_OUTPUT="${BASE_REGISTRY}/${BASE_REPOSITORY}"
BASE_OUTPUT_CLOUD="${BASE_REGISTRY_CLOUD}/${BASE_REPOSITORY}"

GPU_CONTAINER="${BASE_REGISTRY}/nvidia-cuda/devel:${CUDA_VERSION}-ubuntu${UBUNTU_VERSION}"
GPU_OUTPUT="${BASE_OUTPUT}-gpu"
GPU_OUTPUT_CLOUD="${BASE_OUTPUT_CLOUD}-gpu"

