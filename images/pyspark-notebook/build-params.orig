source ../build-params

BASE_REPOSITORY="jupyter/pyspark-notebook:latest"

BASE_CONTAINER=${BASE_REGISTRY}/"jupyter/scipy-notebook:latest"
BASE_OUTPUT="${BASE_REGISTRY}/${BASE_REPOSITORY}"
BASE_OUTPUT_CLOUD="${BASE_REGISTRY_CLOUD}/${BASE_REPOSITORY}"

GPU_CONTAINER="${BASE_CONTAINER}-gpu"
GPU_OUTPUT="${BASE_OUTPUT}-gpu"
GPU_OUTPUT_CLOUD="${BASE_OUTPUT_CLOUD}-gpu"
